# Milaap dashboard

This Next.js application provides the public complaint form, the administrator control center, and the authority alert portal. See the [root README](../README.md) for full environment, setup, authentication, and run instructions.

Commands:

```bash
npm install
npm run dev
npm run lint
npm test
```
